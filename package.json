{
  "name": "express-websocket-proxy",
  "version": "1.0.0",
  "description": "recieves requests though express and sends them through a websocket to the target client, running on a local pc with a dynamic ip address",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "debug": "node ./src/index.js debug",
    "start": "node ./src/index.js"
  },
  "author": "Tare",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.5.2",
    "socket.io-client": "^4.5.2",
    "uuid": "^9.0.0"
  }
}
